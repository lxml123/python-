
python内置数据结构
一、数值型
  int：整数，
  float:有整数和小数部分组成。支持十进制和科学计数法表示。只有双精度型。
  comlex:有实数和虚数部分组成，实数和虚数部分都是浮点数。
  bool:int的子类（也属于整数），仅有2个实例True False对应1和0，可以和整数直接运算。
  
  int(x)返回一个整数，不管小数部分
  float（x）返回一个浮点数
  comlex(x)、comlex(x,y)返回一个复数
  bool（x）返回布尔值
  round(x)四舍六入五取偶
  
二、数字的处理函数
  max()
  min()
  pow(x,y) = x**y
  math.sqrt() 开方
  
  进制函数，返回字符串
  bin()二进制
  oct()十进制
  hex()十六进制
  
  类型判断
  type(obj),返回类型，而不是字符串
  isinstance(obj,class_or_tuplr),返回布尔值
  
  列表list[]
  列表内的个体称作元素，由若干个元素组成
  元素可以是任意对象（数字，字符串，对象，列表等）
  列表内元素有顺序，可以使用索引
  线性的数据结构
  列表索引访问：正索引，从左往右从0开始；负索引，相反，从-1开始
  
  列表查询
  index（value,[start，[stop]]）
       通过
  
 
